
const ProductsInfo = ({items, title, image}) => {
    return (
        <div>
            <div>
                {
                    items.map( product => ())
                }
            </div>
        </div>
    );
};

export default ProductsInfo;